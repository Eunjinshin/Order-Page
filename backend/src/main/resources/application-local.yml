spring:
  jpa:
    show-sql: true

  datasource:
    url: jdbc:mysql://localhost:3306/odersite?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: odersite
    password: odersite123

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.odersite: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
